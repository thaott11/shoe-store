
<div class="container mt-5">
    @*  <div class="row">
    <!-- Main Product Image -->
    <div class="col-md-6">
    <div id="productImageCarousel" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
    @if (Model.SanPhamAnhChiTiets != null && Model.SanPhamAnhChiTiets.Any())
    {
    var mainImage = Model.SanPhamAnhChiTiets.FirstOrDefault(); // Assuming the first image is the main one
    if (mainImage != null)
    {
    <div class="carousel-item active">
    <img src="data:image/jpeg;base64,@(Convert.ToBase64String(mainImage.HinhAnh))" class="d-block w-100" alt="Main Product Image">
    </div>
    }
    }
    else
    {
    <div class="carousel-item active">
    <img src="https://via.placeholder.com/800x400" class="d-block w-100" alt="No image available">
    </div>
    }
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#productImageCarousel" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#productImageCarousel" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
    </button>
    </div>

    <!-- Additional Images Section -->
    <div class="mt-3 d-flex">
    @if (Model.SanPhamAnhChiTiets != null && Model.SanPhamAnhChiTiets.Any())
    {
    foreach (var image in Model.SanPhamAnhChiTiets.Skip(1)) // Skip the main image
    {
    <img src="data:image/jpeg;base64,@(Convert.ToBase64String(image.HinhAnh))" class="img-thumbnail" alt="Additional Image" style="width: 70px; margin-right: 10px;">
    }
    }
    </div>
    </div>

    <!-- Product Details -->
    <div class="col-md-6">
    <h3>@Model.Ten</h3>
    <p class="text-muted">Nhà sản xuất: @Model.NhaSanXuat</p>
    <h4 class="text-primary">@Model.Gia.ToString("N0") VND</h4>
    <p>@Model.MoTa</p>

    <div class="color-selection">
    <h6>Select Color</h6>
    <!-- Replace with actual color options if available -->
    <div class="d-flex">
    <img src="https://via.placeholder.com/50" class="img-thumbnail" alt="Color Option" style="width: 50px; margin-right: 10px;">
    <img src="https://via.placeholder.com/50" class="img-thumbnail" alt="Color Option" style="width: 50px;">
    </div>
    </div>

    <div class="size-selection mt-4">
    <h6>Select Size</h6>
    <div class="d-flex flex-wrap">
    @if (Model.sanPhamSizes != null && Model.sanPhamSizes.Any())
    {
    foreach (var size in Model.sanPhamSizes)
    {
    <button class="btn btn-outline-secondary m-1">@size.Size</button>
    }
    }
    else
    {
    <p>No sizes available</p>
    }
    </div>
    </div>

    <div class="mt-4">
    <button class="btn btn-primary">Add to Cart</button>
    </div>
    </div>
    </div> *@
</div>
